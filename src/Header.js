import React from 'react';

const Header = () => {
  return (
    <header className="App-header">
      <h1>Home Dashboard</h1>
      <div>Dashboard</div>
    </header>
  );
};

export default Header;
